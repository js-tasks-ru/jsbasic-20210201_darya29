/**
 * checkSpam
 * @param {string} str base
 * @returns {boolean}
 */
function checkSpam(str) {
  str = str.toUpperCase().indexOf('xxx'.toUpperCase()) && str.toUpperCase().indexOf('1xbet'.toUpperCase()) ? false : true;
  retern (str);
  }

  checkSpam(str);
